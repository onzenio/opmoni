## 1. Baseline e domínio backend

- [x] 1.1 Registrar baseline (git status limpo em `feat/work` sobre o main 6807f1e, suite backend verde, lint/typecheck verde) e confirmar versões instaladas (Laravel, Nuxt, Nuxt UI) sem adicionar dependências.
- [x] 1.2 Escrever testes Feature failing de Work (TDD RED): CRUD de template com blueprint completo, regra (regimes+tags) com exceções added/removed, preview de elegíveis, geração manual idempotente por (template, cliente, mês), comando agendado, congelamento do gerado, guarda de cascata (dispensada libera a seguinte), ciclo A fazer→Em progresso→Concluída/Dispensada (motivo obrigatório), policies por nível, isolamento entre accounts e calendário omitindo tasks sem prazo; verificar que falham pelo comportamento ausente esperado.
- [x] 1.3 Criar enums `TaskStatus` (`todo|doing|done|dismissed`) e `TaskPriority` (`low|medium|high|urgent`) e migration evoluindo `processes` (`client_id` nullable FK, `template_id` nullable FK, `reference_month` nullable dia 01, `status`, `due_on` nullable, unique `account+template+cliente+mês`) mais tabelas `process_templates` (+`cascade`, `generate_day`, `due_day`, `is_active`, `regimes` JSON), `process_template_tasks` (+`department`, `due_day`, `priority`), `template_tag`, `template_client_exceptions` (`added|removed`), `tasks` (link só via `process_id`, sem `client_id` próprio, `dismissal_reason` nullable) com FKs e cascatas; verificar migrations em SQLite de teste e borda de `due_day` 31 em fevereiro.
- [x] 1.4 Implementar models `ProcessTemplate`, `ProcessTemplateTask`, `Task` (concern `BelongsToAccount`, casts, scopes por status/responsável/departamento/prioridade/vencimento) e relações em `Process`/`Client`/`Account`/`Tag`, com factories; verificar testes de ownership tenant e ordenação (vencimento nulls-last, ordem).
- [x] 1.5 Implementar `ProcessGenerationService::generate` (resolução da regra em query única + exceções, find-or-create transacional por (template, cliente, mês) com lock, snapshot da etapa nas tasks com `due_on` = dia fixo limitado ao mês, status inicial `todo`) e comando `work:generate-recurrences` + agendamento diário; verificar idempotência sob repetição, teste do comando e congelamento (mudança de regra pós-geração não altera o mês).

## 2. API tenant Work

- [x] 2.1 Implementar `ProcessTemplateController` (CRUD + sync de blueprint + sync de regra: regimes, tag ids, exceções added/removed) com Form Requests e Resources, policies espelhando `ProcessPolicy` e `SupportAudit::logWrite`; verificar 201/200/403/404, 422 para exceção de cliente alheio e isolamento entre accounts.
- [x] 2.2 Evoluir `ProcessController` (filtros por template/mês/cliente/status, ordenação por mês desc, detalhe com cliente + tasks ordenadas e progresso derivado total/concluídas/dispensadas/abertas/razão); verificar listagem, detalhe e progresso parcial via testes.
- [x] 2.3 Implementar `TaskController` (listagem com filtros processo/cliente via processo/status/responsável/departamento/prioridade/faixa de vencimento, atualização de status com carimbo/limpeza de `completed_at`, motivo obrigatório ao dispensar, guarda de cascata recusando furar sequência salvo antecessora concluída ou dispensada, atribuição validada a membro do account); verificar ciclo A fazer→Em progresso→Concluída/Dispensada, reabertura, bloqueio de cascata, 403 para `user`.
- [x] 2.4 Implementar `POST /process-templates/{id}/generate` (manual, mesmo serviço do scheduler), `GET .../preview` (elegíveis com motivo, sem criar nada) e `GET /work/calendar?from&to` (só tasks com `due_on`) mais payload agrupado Cliente > Processo > Task com totais e razões; verificar geração manual para N clientes, idempotência, feed sem tasks sem prazo e forma do agrupado.
- [x] 2.5 Registrar policies `ProcessTemplatePolicy`/`TaskPolicy` em `AppServiceProvider`, rotas em `routes/api.php` e atualizar testes de suporte/isolamento existentes afetados; verificar suite backend completa verde.

## 3. Frontend base Work

- [x] 3.1 Criar seção Work no sidebar (`layouts/default.vue`), layout `app/pages/work.vue` (UDashboardPanel/Navbar/Toolbar com tabs), util `app/utils/workNav.ts` (rotas das 5 visões, padrão `monitoringNav.ts`) e composable `app/composables/useWork.ts` (templates, processos, tasks, calendário, agrupado, preview, tipagem pt-BR); verificar typecheck verde e navegação entre as 5 abas.
- [x] 3.2 Criar páginas `work/calendario`, `work/clientes`, `work/processos`, `work/tarefas`, `work/modelos` (+ detalhes `processos/[id]`, `modelos/[id]`) com mocks tipados e estados loading/empty/error (UEmpty/USkeleton/UAlert, toasts pt-BR); verificar `pnpm lint` e `pnpm typecheck` verdes.

## 4. Visões Work integradas

- [x] 4.1 Integrar calendário: `UCalendar` com slot `#day` e `UChip` por status (padrão CalendarEventsExample) + lista de tasks do dia + filtros processo/responsável/status consumindo o feed real; verificar só tasks com prazo aparecem e filtros funcionam.
- [x] 4.2 Integrar visão cliente: `UTable` com `getGroupedRowModel`, `grouping ['client_id','process_id']`, coluna `title` com expand + nome do cliente/processo + `UBadge` de status (cores todo:info, doing:warning, done:success, dismissed:neutral); verificar agrupamento Cliente > Processo > Task com dados reais.
- [x] 4.3 Integrar processos: tabela por competência e cliente + detalhe com Stepper/Timeline de progresso e tasks ordenadas expansíveis (padrão TableRowExpandableExample, cadeado onde a cascata bloqueia); verificar progresso derivado e navegação lista→detalhe.
- [x] 4.4 Integrar tarefas: kanban em 4 colunas (A fazer/Em progresso/Concluída/Dispensada) com cards fiscais (processo+mês, cliente, vencimento, responsável, prioridade, departamento, cadeado de cascata) e botões avançar/retornar/dispensar (com motivo) via API (sem drag-and-drop na v1) + alternância tabela, compartilhando os filtros da listagem e atribuição via `USelectMenu` de membros; verificar transições, bloqueio com feedback, contadores por coluna e 403 visual para `user` (sem ações de escrita).
- [x] 4.5 Integrar modelos: tabela estilo TaskHub (Título, Regimes, Categorias/Tags, Departamentos, Clientes associados, Recorrência) + editor em abas (Associação, Clientes e Exceções, Prazo, Tarefas, Recorrência) + preview de elegíveis + botão "gerar mês"; verificar criação fim-a-fim gerando um processo por cliente do mês com snapshot congelado.

## 5. Verificação e release

- [x] 5.1 Rodar `vendor/bin/pint --dirty --format agent` e suite backend completa; verificar formatação e todos os testes verdes.
- [x] 5.2 Rodar `pnpm lint`, `pnpm typecheck` e `pnpm build` no frontend; verificar verdes ou com baseline de falhas alheias documentada.
- [x] 5.3 Executar cenários manuais autenticados (modelo PGDAS com regra Simples + exceção, 10 empresas gerando 10 processos, repetição sem duplicar, scheduler, troca de regime congelando o gerado, cascata bloqueando, isolamento entre accounts, níveis, auditoria de suporte, calendário, board); verificar HTTP, estado persistido e audit log conformes às specs.
